# Task Completion Checklist

1. Run `npx tsc --noEmit` to verify TypeScript compilation
2. Run relevant test scripts from `test_scripts/`
3. Update CLAUDE.md with any new tools/commands (XML format required)
4. Update `docs/design/project-design.md` with design changes
5. Update `docs/design/project-functions.md` with new functional requirements
6. Update `Issues - Pending Items.md` at project root
7. Update memory file at `~/.claude/projects/.../memory/MEMORY.md`
